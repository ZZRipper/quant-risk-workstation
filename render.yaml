services:
  - type: web
    name: quant-risk-workstation
    runtime: static
    buildCommand: python3 -m pip install -r requirements.txt && python3 scripts/build_dashboard_data.py && python3 scripts/prepare_public.py
    staticPublishPath: public
